<%= tinymce_assets %>

<%= simple_form_for blog do |f| %>
  <%= f.input :title %>
	<%= f.input :content, as: :text, :input_html => { class: 'tinymce text-edit' } %>
  <%= f.button :submit %>
<% end %>

<%= tinymce %>
