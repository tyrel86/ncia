<%= tinymce_assets %>

<%= simple_form_for article do |f| %>
  <%= f.input :title %>
	<%= f.input :content, as: :text, :input_html => { class: 'tinymce text-edit' } %>
	<%= f.input :author %>
	<%= f.input :source %>
	<br />
  <%= f.button :submit %>
<% end %>

<%= tinymce %>
